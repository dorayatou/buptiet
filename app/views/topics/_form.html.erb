<%= form_for(@topic, :html =>{ "data-ajax" => false }) do |f| %>
  <% if @topic.errors.any? %>
    <div>
      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <span class="graytitle">投票主体:</span>
  <ul class="pageitem">
    <li class="bigfield">
      <%= f.text_field :body, :size => 20, "placeholder" => "Body", "type" => "text" %>
    </li>
  </ul>

  <ul class="pageitem">
      <li class="button"><%= submit_tag "确认", "type" => "submit" %></li>
  </ul>
<% end %>
